/////////////////////////////////////////////// PATRON PARA RECONOCIMIENTO DE CALLES
Phase: StreetPhase
Input: Token Country
Options: control = first


Rule:StreetPattern1
(
	(
	({Token.kind == number}
	{Token.string == ","} )?
	{Token.string =~ "^[Rr]ue"}
	({Token} )[1,14]
	({Country} )

	):st
)

-->
:st.Street = {kind = "StreetPattern1",string = :st@cleanString}
