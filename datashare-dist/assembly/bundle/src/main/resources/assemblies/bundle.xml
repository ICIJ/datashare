<assembly>

    <id>bundle</id>

    <formats>
        <format>zip</format>
    </formats>

    <includeBaseDirectory>false</includeBaseDirectory>

    <fileSets>

        <!-- src -->
        <fileSet>
            <directory>src/main/resources</directory>
            <outputDirectory>src/main/resources</outputDirectory>
            <useDefaultExcludes>true</useDefaultExcludes>
            <excludes>
                <exclude>org/icij/datashare/text/extraction/tika/parser/ocr/**</exclude>
                <exclude>org/icij/datashare/text/nlp/open/models/**</exclude>
                <exclude>**/*.so</exclude>
            </excludes>
        </fileSet>

        <!-- opt -->
        <fileSet>
            <directory>opt</directory>
            <outputDirectory>opt</outputDirectory>
            <useDefaultExcludes>true</useDefaultExcludes>
        </fileSet>

        <!-- scr -->
        <fileSet>
            <directory>scr</directory>
            <outputDirectory>./</outputDirectory>
            <includes>
                <include>start*</include>
                <include>stop*</include>
            </includes>
        </fileSet>

    </fileSets>

</assembly>